# Лабароторная работа 2
### Задание 1
C++: int, auto, float, for, void.\
Java: int, while, do, for, float.\
Python: for, in, import, return, def.
### Задание 2
C++:\
int\
main\
(\
)\
{\
int\
a\
=\
1\
,\
b\
=\
2\
;\
int\
c\
=\
a\
++\
b\
;\
return\
0\
;\
}

Python:\
a\
=\
1\
b\
=\
2\
c\
=\
a\
+\
+\
b

В C++ есть токен "++", он увеличивает переменную a на 1, а после идёт переменная b без всего, из-за этого возникает ошибка.

В Python токена "++" нет, но есть токен одноместный токен "+", благодаря этому выражение превращается в обычное сложение, где один из токенов "+" просто вернёт одну из переменных, а второй сложит две переменных.
### Задание 3
Трансляция и запуск программы после добавления в неё вывода всех трёх переменных без добавления пробелов.\
![03](03/03.png)\
Трансляция и запуск программы после изменения переменной c на "int c=a++ +b".\
![03](03/03_1.png)\
Трансляция и запуск программы после изменения переменной c на "int c=a+ ++b".\
![03](03/03_2.png)\
Как можно видеть из этих скриншотов место пробела меняет разбиение строки "+++" на два разных набора токенов: "++", "+" и "+", "++".\
Разбивать на токены я буду исходную программу (без добавления вывода).\
int\
main\
(\
)\
{\
int\
a\
=\
1\
,\
b\
=\
2\
;\
int\
c\
=\
a\
++\
+\
b\
;\
return\
0\
;\
}
### Задание 4
Круглые скобочки после имени - оператор вызова функции.\
Например input()\
Круглые скобочки в другом случае - пунктуатор.\
Например ((a+)++b)\
Квадратные скобочки для индексации - оператор.\
Например v[i]\
Квадратные скобочки в другом случае - пунктуатор.\
Здесь пример привести сложно, так как квадратные скобки во многих языках закреплены за конкретными операциями.
### Задание 5
Ключевые слова: for, in\
Идентификаторы: print, map, sum, zip, int, input, i, split\
Литералы: 1, 2, 3\
Операторы: *, ,, (), [], .

Имена переменных: i\
Имена функций: print, map, sum, zip, int, input\
Имена методов: split

Функции и их параметры:\
*input() параметров нет, принимает на вход данные\
*split() параметров нет, разделяет полученную строку на список строк разделённых пробелами\
*int() параметр это строка из input, возвращает строку как целочисленное значение\
*внешний map() параметр sum, применяет его ко всем zip\
*внутренний map() параметр int, применяет int ко всем input\
*zip() параметр это *[map(int, input().split()) for i in (1,2,3)], функция возвращает итератор кортежей объединяющих элементы по индексу\
*sum() параметры это кортеж чисел от zip и возвращает их сумму\
*print() параметр это всё выражение, выводит сообщение

Эта программа считывает 3 строки ввода (каждая строка содержит числа, разделенные пробелами), вычисляет сумму чисел в каждом столбце и выводит полученные суммы через пробел.
### Задание 6
Комментарии можно записывать только четвёртым вариантом. 

Первый вариант выводит текст комментария, так что это не комментарий, а просто текст в выводе, остальные не валидные конструкции.

Во второй комментируется открывающий токен комментария, из-за чего коммент не создаётся.

В третьем варианте открывающий токен защитывается как часть комментария и поэтому второй закрывающий токен остаётся без открывающего.

Последний вариант закрывает комментарий на уже закоментированной строке, значит второй закрывающий токен остаётся без открывающего
### Задание 7
Как видно из правил, есть 4 инструкции циклов.

Первый вид — цикл while (с предусловием), который соответствует строке правила:
while ( condition ) statement

Формат записи (по правилу): ключевое слово while, в круглых скобках условие condition, далее инструкция statement.

Пример фрагмента программы:

int x = 1;\
while (x < 1000) {\
    cout << x;\
    x *= 2;
}

Второй вид — цикл do-while (с постусловием), который соответствует строке правила:
do statement while ( expression ) ;

Формат записи (по правилу): ключевое слово do, далее инструкция statement, ключевое слово while, в круглых скобках выражение expression, завершается точкой с запятой ;.

Пример фрагмента программы:

int x = 1;\
do {\
    cout << x;\
    x *= 2;\
} while (x < 1000);

Третий вид — классический цикл for (с инициализацией, условием и итерацией), который соответствует строке правила:
for ( init-statement condition_opt ; expression_opt ) statement

Формат записи (по правилу): ключевое слово for, в круглых скобках: инструкция инициализации init-statement, необязательное условие condition_opt, символ ;, необязательное выражение expression_opt. Далее инструкция statement.

Пример фрагмента программы:

for (int i = 0; i < 10; ++i) {\
    cout << i;\
}

Четвертый вид — диапазонный цикл for (range-based for), который соответствует строке правила:
for ( for-range-declaration : for-range-initializer ) statement

Формат записи (по правилу): ключевое слово for, в круглых скобках: объявление переменной диапазона for-range-declaration, двоеточие :, инициализатор диапазона for-range-initializer. Далее инструкция statement.

Пример фрагмента программы:

int arr[] = {1, 2, 3};\
for (int x : arr) {\
    cout << x;\
}
### Задание 8
          =
         / \
        a   +
           / \
          -   *
         /   / \
        x   y   -
               / \
              z   1